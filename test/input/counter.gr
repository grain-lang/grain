let counter = {
  let count = box(0);
  let rec counter_help = () => {
    count := unbox(count) + 1
  };
  counter_help
};
{
  # TODO: This should have some equivalent of OCaml's ignore(...)
  print(counter());
  print(counter());
  print(counter())
}
