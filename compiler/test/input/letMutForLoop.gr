import WasmI64 from "runtime/unsafe/wasmi64"
import WasmUtils from "runtime/unsafe/utils"

@disableGC
let foo = () => {
  for (let mut x = 0N; WasmI64.ltS(x, 5N); x = WasmI64.add(x, 1N)) {
    WasmUtils.printI64(x)
  }
}

foo()
