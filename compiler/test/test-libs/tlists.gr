module TLists

expose enum TList<a> {
  Empty,
  Cons(a, TList<a>),
}

expose let rec sum = l => {
  match (l) {
    Empty => 0,
    Cons(a, more) => a + sum(more),
  }
}

expose let rec append = (l1, l2) => {
  match (l1) {
    Empty => l2,
    Cons(hd, tl) => Cons(hd, append(tl, l2)),
  }
}
