module Operators

from "list" include List

let myList = []

List.reduce((&&), true, myList)

let a = true
let b = true && false
let c = false

!b

if (!b) print("false")

if (a && b) print("true")

if (a && (b || c)) print("who knows")
