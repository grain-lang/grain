module Chained

let g = 7
and p = 8
and q = 32

let myFunction2 = x => {
  let inter = x + 1
  "some string"
}
and myFunction3 = y => {
  let myVal = 5
  "some string"
}

let g = 7
and p = 8
and q = 32

let myFunction2 = x => {
  let inter = x + 1
  "some string"
}
and myFunction3 = y => {
  let myVal = 5
  "some string"
}
