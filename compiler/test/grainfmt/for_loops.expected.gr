module ForLoops

let size = 2

for (let mut i = 0; i < size; i += 1) {
  let strLength = 0
}

let verylongvariablenameforcingalinebreakhereplease = 2

for (
  let mut i = 0;
  i < verylongvariablenameforcingalinebreakhereplease;
  i += 1
) {
  let strLength = 0
  print("Loop")
}

for (let mut i = 0; i < size; i += 1) print("Loop")
