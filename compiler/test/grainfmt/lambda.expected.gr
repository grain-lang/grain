module Lambda

x => {
  x + 1
}

y => {
  let x = 1
  let z = 2
}

let testfna = forEachCodePoint(c => void)

let testfn = forEachCodePoint(c => {
  void
}, "")

let testfn3 = forEachCodePoint((c, d, e) => {
  void
}, "")

let testfn4 = forEachCodePoint(c => {
  void
}, "")

let testfn5 = forEachCodePoint(c => {
  void
}, "")
