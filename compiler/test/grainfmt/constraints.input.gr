module Constraints

from "list" include List
let test = test => {
  // Comments
  List.forEach(i => {
    // Comment In ForEach
    print(i)
  }, test: List<Number>)
}

// Regression #2244
(1: Number): Number

((1: Number): Number): Number
