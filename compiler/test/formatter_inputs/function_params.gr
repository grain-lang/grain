let single_arg = (x) => x

let unit_arg = () => 3

let two_args = (x,y) => 4

export let fd_write: (WasmI32, WasmI32, WasmI32, WasmI32) -> WasmI32 =
  (fd,
    iovs,
    iovs_len,
    nwritten) => {
  0n
}